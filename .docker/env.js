(function (w) {

    w.ENV = w.ENV || {};

    w.ENV.DOCUMENT_TITLE = '${DOCUMENT_TITLE}';
    w.ENV.IPFS_GATEWAY = '${IPFS_GATEWAY}';
    w.ENV.SCAN_URL = '${SCAN_URL}';
    w.ENV.UNIQUE_API_URL = '${UNIQUE_API_URL}';
    w.ENV.RAMP_API_KEY = '${RAMP_API_KEY}';

})(this);
